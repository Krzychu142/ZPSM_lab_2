// exercise one 
const _ = require('lodash');

let testArray = [1, 2, 3, 4, 5];
console.log(_.mean(testArray));

console.log(_.min(testArray));
console.log(_.max(testArray));

// in second exercise my branch is called "develop" insted of "master", sorry for that, I didn't see this order and like always I made develop branch as default
